import styled from "styled-components";

export const Wrapper = styled.div`
  display: flex;
  padding-top: 5rem;
  overflow: hidden;
  .slick-list {
    border-radius: 3rem;
  }
  .slick-slide {
    padding: 0 10px;
  }
`;
